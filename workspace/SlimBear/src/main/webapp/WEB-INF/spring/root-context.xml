<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xmlns:jdbc="http://www.springframework.org/schema/jdbc"
	xmlns:aop="http://www.springframework.org/schema/aop"
	xmlns:c="http://www.springframework.org/schema/c"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:p="http://www.springframework.org/schema/p"
	xsi:schemaLocation="http://www.springframework.org/schema/jdbc http://www.springframework.org/schema/jdbc/spring-jdbc-4.3.xsd
		http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.3.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd
		http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-4.3.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-4.3.xsd">

	<context:component-scan
		base-package="com.kgitbank.slimbear">
		<context:exclude-filter type="regex"
			expression="com.kgitbank.slimbear.controller.*" />
		<context:exclude-filter type="regex"
			expression="com.kgitbank.slimbear.admin.controller.*" />
	</context:component-scan>

	<!-- 설정 파일 불러오기 -->
	<context:property-placeholder
		location="classpath:application.properties,classpath:secret.properties" />

	<!-- DBCP DataSource -->
	<bean id="dataSource"
		class="org.apache.commons.dbcp2.BasicDataSource">
		<property name="driverClassName" value="${jdbc.driverClass}" />
		<property name="url" value="${jdbc.url}" />
		<property name="username" value="${jdbc.username}" />
		<property name="password" value="${jdbc.password}" />
	</bean>

	<!-- Mybatis -->
	<bean id="sqlSessionFactory"
		class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />
		<property name="configLocation" value="${mybatis.configLocation}" />
		<property name="mapperLocations" value="${mybatis.mapperLocations}" />
	</bean>
	<bean id="sqlSessionTemplate"
		class="org.mybatis.spring.SqlSessionTemplate">
		<constructor-arg name="sqlSessionFactory"
			ref="sqlSessionFactory" />
	</bean>

	<!-- Transaction -->
	<bean id="transactionManager"
		class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="dataSource"></property>
	</bean>

	<!-- Transaction Advice -->
	<!-- Advice를 사용하지 않고 @Transactional 어노테이션으로 사용할 수도 있습니다. -->
	<tx:advice id="transactionAdvice"
		transaction-manager="transactionManager">
		<tx:attributes>
			<tx:method name="get*" read-only="true" /> <!-- get으로 시작하는 메소드는 transaction을 적용하지 않는다 -->
			<tx:method name="*"/>
		</tx:attributes>
	</tx:advice>

	<aop:config>
        <!-- Transaction Pointcut-->
        <aop:pointcut id="transactionPoincut" expression="execution(* com.kgitbank.slimbear.service.*Impl.*(..))" />
        <!-- Transaction Aspect-->
        <aop:advisor advice-ref="transactionAdvice" pointcut-ref="transactionPoincut" />
    </aop:config>
    
	<!-- 파일 업로드 -->
	<bean id="multipartResolver"
		class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<property name="defaultEncoding" value="${file-upload.defaultEncoding}" ></property>
		<property name="maxUploadSize"  value="${file-upload.maxUploadSize}" ></property>
		<property name="maxInMemorySize" value="${file-upload.maxInMemorySize}" ></property>
	</bean>
	
	
	<!-- iamport 결제대행 -->
	<bean id="slimBearImportPayment"
		class="com.kgitbank.slimbear.common.SlimBearImportPayment">
			<constructor-arg name="apiKey" value="${imp.api.key}"></constructor-arg>
			<constructor-arg name="apiSecret" value="${imp.api.secretkey}"></constructor-arg>
	</bean>

</beans>
