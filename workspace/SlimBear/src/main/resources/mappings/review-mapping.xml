<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.slimbear.mapper.Review">
 
 	<!-- 리뷰 전체 목록 조회 -->
 	<select id="SELECT_REVIEW_LIST" resultType="review">
 		SELECT * FROM productReview
 	</select>
 	
 	<!-- 리뷰 최신 순 조회 -->
 	<select id="SELECT_REVIEW_RECENT" resultType="review">
 		SELECT * FROM productReview ORDER BY reg_date DESC
 	</select>
 	
 	<insert id="INSERT_REVIEW">
 		INSERT INTO productReview
 		(uid, mem_id, prod_code, title, content, score, reg_date)
 		VALUES(#{uid}, #{mem_id}, #{prod_code}, #{title}, #{content}, #{score}, #{reg_date})
 	</insert>
 </mapper>